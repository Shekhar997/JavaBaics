
public interface Perimeter {
	
	public void peri();
	
	public void area(int num);
	//public void area(); // this is possible as both interface have the same the same method. But since method are abstract so it won't create the issue
	
	
}
